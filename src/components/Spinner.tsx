const Spinner = () => {
    return (
        <div>
            <div className="flex items-center justify-center min-h-screen bg-white">
                <div className="size16 border-4 border-custom-color2 border-t-transparent rounded-full animate-spin" />
            </div>
        </div>
    );
};

export default Spinner;
